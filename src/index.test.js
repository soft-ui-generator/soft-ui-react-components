import { ExampleComponent, Button } from '.'

describe('ExampleComponent', () => {
  it('is truthy', () => {
    expect(ExampleComponent).toBeTruthy()
  })
})

describe('Button', () => {
  it('is truthy', () => {
    expect(Button).toBeTruthy()
  })
})
